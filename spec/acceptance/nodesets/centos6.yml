HOSTS:
  centos6:
    ip: 192.168.0.1
    roles:
      - master
    platform: el-6-x86_64
    image: centos:6
    hypervisor: docker
    docker_image_commands:
      - 'cat /etc/resolv.conf'
      - 'yum -y install gcc tar'
    docker_cmd: '["/sbin/init"]'
CONFIG:
  type: foss
  log_level: verbose
