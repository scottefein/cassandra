machine:
#  services:
#    - docker

test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) rake lint ;; 1) rake validate ;; esac:
        parallel: true
#    - case $CIRCLE_NODE_INDEX in 0) bash circle.bash unit_tests ;; 1) bash circle.bash unit_tests ;; 2) bash circle.bash unit_tests ;; 3) bash circle.bash unit_tests ;; esac:
        parallel: true

general:
  branches:
    only:
      - circleci

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    # gitter hook
    - url: https://webhooks.gitter.im/e/da8c065419a91983f0cf
